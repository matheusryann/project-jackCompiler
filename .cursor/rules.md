# Regras do Cursor

As regras do projeto estão em **`.cursor/rules/`** (ficheiros `.mdc` com frontmatter).

- `jack-compiler-core.mdc` — aplica-se sempre (Java, Nand2Tetris, TDD; agente inclui testes no ciclo).
- `jack-lexical-documentation.mdc` — ao trabalhar em `jackcompiler/` ou testes, manter `docs/analisador-lexico.md` alinhado.
